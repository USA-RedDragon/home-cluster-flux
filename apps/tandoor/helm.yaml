apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tandoor
  namespace: tandoor
spec:
  interval: 60m0s
  driftDetection:
    mode: enabled
  releaseName: tandoor
  suspend: true
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: '3.6.1'
      interval: 60m0s
  valuesFrom:
    - kind: ConfigMap
      name: chart-values
