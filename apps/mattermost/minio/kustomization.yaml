apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mattermost
resources:
  - helm.yaml
configMapGenerator:
  - name: minio-chart-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
