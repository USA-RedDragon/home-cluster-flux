apiVersion: gateway.networking.k8s.io/v1alpha3
kind: BackendTLSPolicy
metadata:
  name: scrypted-cloud-tls
  namespace: scrypted
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: scrypted
      sectionName: cloud
  validation:
    caCertificateRefs:
      - name: scrypted-ca
        kind: Secret
        group: ""
    hostname: cloud.nvr.mcswain.dev
