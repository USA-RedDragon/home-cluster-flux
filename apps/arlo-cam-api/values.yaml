# Default values for app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/usa-reddragon/arlo-cam-api
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "main@sha256:acd1b60f95dc65fac36fc25366c1a53528a0f2de3369e22ccfd71fe2fb8de213"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# command:
# - /scripts/estimate_rtlsdr_ppm.sh

secrets: []
# - name: app-creds
#   data:
#     API_KEY: 'asdfgh123456'
#     SMTP_PASSWORD: '123456asdfgh'
# - name: app-secret-key
#   data:
#     SECRET_KEY: ashdfsfa
# - name: app-keyfile
#   data:
#     keyfile.txt: |
#       using
#       multiline
#       strings

configs:
- name: arlo-cam-api-config
  data:
    config.yaml: |
      WifiCountryCode: "US"
      VideoAntiFlickerRate: 60
      VideoQualityDefault: "subscription"
      NotifyOnMotionAlert: true
      NotifyOnMotionTimeoutAlert: false
      NotifyOnAudioAlert: false
      NotifyOnButtonPressAlert: true
      MotionRecordingWebHookUrl: "http://scrypted.scrypted.svc.cluster.local:11080/endpoint/@scrypted/arlo-local/public/motionDetected"
      MotionTimeoutWebHookUrl: ""
      AudioRecordingWebHookUrl: ""
      UserRecordingWebHookUrl: ""
      StatusUpdateWebHookUrl: "http://scrypted.scrypted.svc.cluster.local:11080/endpoint/@scrypted/arlo-local/public/statusUpdated"
      RegistrationWebHookUrl: "http://scrypted.scrypted.svc.cluster.local:11080/endpoint/@scrypted/arlo-local/public/registered"
      ButtonPressWebHookUrl: "http://scrypted.scrypted.svc.cluster.local:11080/endpoint/@scrypted/arlo-local/public/buttonPressed"
# - name: app-otherenv
#   data:
#     SPECIAL_KEY: here
# - name: app-configfile
#   data:
#     test.ini: |
#       [test]
#       value=1

envFrom: []
# - configMapRef:
#     name: app-config

env:
- name: DB_PATH
  value: /db/arlo.db

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 999

securityContext:
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 999
  # runAsGroup: 999
  # fsGroup: 999

service:
  type: LoadBalancer
  internalTrafficPolicy: Cluster
  ports:
  - name: http
    port: 5000
  - name: cam-api
    port: 4000
  annotations:
    lbipam.cilium.io/ips: 192.168.254.10
  loadBalancerClass: io.cilium/l2-announcer

persistentVolumes:
- name: config
  mountPath: /opt/arlo-cam-api/config.yaml
  subPath: config.yaml
  configMap:
    name: arlo-cam-api-config
- name: db
  mountPath: /db
  size: 1Gi
  persistentVolumeClaim:
    name: db
    storageClass: longhorn-ssd

ingress:
  enabled: true
  ingressClassName: tailscale
  hosts:
  - host: arlo-cam-api
    paths:
    - port: 5000
      path: /
      pathType: Prefix
  tls:
  - hosts:
      - arlo-cam-api

probes: {}

resources:
  requests:
    cpu: 25m
    memory: 50Mi

nodeSelector: {}

tolerations: []

affinity: {}

lifecycle: {}
  # postStart:
  #   exec:
  #     command:
  #       - /bin/sh
  #       - -c
  #       - cp /tmp/file /var/app/file
