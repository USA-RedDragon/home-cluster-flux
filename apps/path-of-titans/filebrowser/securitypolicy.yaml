apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: filebrowser
  namespace: path-of-titans
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: filebrowser
  oidc:
    provider:
      issuer: https://auth.srshost.ing/application/o/path-of-titans-files/
    clientIDRef:
      name: filebrowser-oidc
    clientSecret:
      name: filebrowser-oidc
    scopes: [openid, profile, email, offline_access]
    refreshToken: true
    defaultTokenTTL: 8h
    defaultRefreshTokenTTL: 8h
