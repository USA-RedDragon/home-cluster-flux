apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cobblemon
  namespace: minecraft
spec:
  install:
    remediation:
      retries: -1
  interval: 60m0s
  releaseName: cobblemon
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: '4.6.2'
      interval: 60m0s
  valuesFrom:
    - kind: ConfigMap
      name: cobblemon-chart-values
