apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: weewx
  namespace: weewx
spec:
  install:
    remediation:
      retries: -1
  interval: 60m0s
  releaseName: weewx
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: '4.6.1'
      interval: 60m0s
  valuesFrom:
    - kind: ConfigMap
      name: chart-values
