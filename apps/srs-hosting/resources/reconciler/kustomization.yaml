apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: srs-hosting
resources:
  - helm.yaml
configMapGenerator:
  - name: reconciler-chart-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
