apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: navidrome
resources:
  - helm.yaml
  - forward-auth.yaml
configMapGenerator:
  - name: navidrome-chart-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
