# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-3.4.0/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json

controllers:
  wheresmyscope:
    replicas: 1
    pod:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65535
        fsGroup: 65535
    containers:
      wheresmyscope:
        image:
          repository: ghcr.io/usa-reddragon/wheresmyscope
          tag: v0.0.1@sha256:cb26be0635993aab2042851c0c36a1c67453742422a4b14715cf00582e327938
          pullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 25m
            memory: 40Mi
        probes:
          liveness:
            enabled: true
          readiness:
            enabled: true
          startup:
            enabled: true
        env:
          - name: MQTT_PREFIX
            value: observatory/mercury/target
          - name: MQTT_BROKER
            value: mqtt://192.168.1.19:1883
          - name: MQTT_USERNAME
            value: wheresmyscope
        envFrom:
          - secretRef:
              name: wheresmyscope
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL

service:
  wheresmyscope:
    controller: wheresmyscope
    ports:
      http:
        port: 8080


ingress:
  wheresmyscope:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: cloudflare
    hosts:
      - host: wheresmyscope.mcswain.dev
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: wheresmyscope
              port: http
    tls:
      - hosts:
          - wheresmyscope.mcswain.dev
        secretName: wheresmyscope-mcswain-dev

networkpolicies:
  wheresmyscope:
    enabled: true
    controller: wheresmyscope
    policyTypes: [Ingress, Egress]
    rules:
      ingress:
        - from:
          - namespaceSelector: {}
      egress:
        - to:
          - namespaceSelector: {}
        - to:
          - ipBlock:
              cidr: 192.168.1.19/32
            port: 1883
        - to:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: kube-system
            podSelector:
              matchLabels:
                k8s-app: kube-dns
