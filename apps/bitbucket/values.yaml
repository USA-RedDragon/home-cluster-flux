# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-3.4.0/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json

controllers:
  bitbucket:
    replicas: 1
    containers:
      bitbucket:
        image:
          repository: jacob.pub/ghcr.io/usa-reddragon/bitbucket
          tag: 10.1.4@sha256:5d3ff8eff51d143ebe5d62775344dd6439618b9972661afa270ca7b58f8186a8
          pullPolicy: IfNotPresent
        resources:
          requests:
            memory: 1Gi
        probes:
          liveness:
            enabled: true
            spec:
              initialDelaySeconds: 120
          readiness:
            enabled: true
            spec:
              initialDelaySeconds: 120
          startup:
            enabled: true
            spec:
              initialDelaySeconds: 120

persistence:
  data:
    enabled: true
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 16Gi
    storageClass: longhorn-ssd
    globalMounts:
      - path: /var/atlassian/application-data/bitbucket

service:
  bitbucket:
    controller: bitbucket
    ports:
      http:
        port: 7990
        primary: true
      ssh:
        port: 7999

route:
  bitbucket:
    enabled: true
    kind: HTTPRoute
    parentRefs:
      - name: public
        namespace: envoy-gateway-system
        sectionName: https
    hostnames:
      - bitbucket.mcswain.dev
    rules:
      - backendRefs:
          - name: bitbucket
            port: 7990
        matches:
          - path:
              type: PathPrefix
              value: /
  ssh:
    enabled: true
    kind: TCPRoute
    parentRefs:
      - name: public
        namespace: envoy-gateway-system
        sectionName: bitbucket-ssh
    rules:
      - backendRefs:
          - name: bitbucket
            port: 7999
