apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: palworld
  namespace: palworld
spec:
  chart:
    spec:
      chart: palworld
      sourceRef:
        kind: HelmRepository
        name: usa-reddragon
        namespace: flux-system
      version: 1.0.1
  interval: 60m0s
  valuesFrom:
    - kind: ConfigMap
      name: chart-values
