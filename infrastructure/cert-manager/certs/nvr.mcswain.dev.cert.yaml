apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nvr.mcswain.dev
spec:
  secretName: nvr.mcswain.dev-tls
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
  dnsNames:
    - nvr.mcswain.dev
    - '*.nvr.mcswain.dev'
