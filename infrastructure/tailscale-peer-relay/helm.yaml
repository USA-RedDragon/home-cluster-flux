---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-peer-relay
  namespace: tailscale-peer-relay
spec:
  install:
    remediation:
      retries: -1
  interval: 60m0s
  releaseName: tailscale-peer-relay
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: '4.6.0'
      interval: 60m0s
  valuesFrom:
    - kind: ConfigMap
      name: chart-values
