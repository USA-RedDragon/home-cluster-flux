apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: backup
  namespace: longhorn-system
spec:
  name: backup
  cron: '0 4 * * ?'
  task: backup
  concurrency: 1
  retain: 100
  groups:
    - default
