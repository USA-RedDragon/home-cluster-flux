apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: reticulum
resources:
  - helm.yaml
configMapGenerator:
  - name: lxmd-chart-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
