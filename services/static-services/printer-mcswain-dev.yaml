apiVersion: v1
kind: Endpoints
metadata:
  name: printer
  namespace: static-services
subsets:
- addresses:
  - ip: 192.168.1.92
  ports:
  - appProtocol: http
    name: http
    port: 80
    protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: printer
  namespace: static-services
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "printer"
    tailscale.com/tags: "tag:k8s"
spec:
  internalTrafficPolicy: Cluster
  ports:
  - name: printer
    port: 80
    protocol: TCP
    targetPort: 80
  sessionAffinity: None
  type: ClusterIP
