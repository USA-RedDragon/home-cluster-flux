apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: printer
  namespace: static-services
  labels:
    kubernetes.io/service-name: printer
    endpointslice.kubernetes.io/managed-by: cluster-admins
addressType: IPv4
endpoints:
  - addresses:
    - 172.29.1.126
    conditions:
      ready: true
ports:
  - name: http
    port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: printer
  namespace: static-services
spec:
  internalTrafficPolicy: Cluster
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  sessionAffinity: None
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: printer
  namespace: static-services
spec:
  ingressClassName: tailscale
  rules:
  - host: printer
    http:
      paths:
      - backend:
          service:
            name: printer
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - printer
