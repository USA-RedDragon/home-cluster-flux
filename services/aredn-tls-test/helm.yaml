apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: aredn-tls-test
  namespace: aredn-tls-test
spec:
  interval: 60m0s
  driftDetection:
    mode: enabled
  releaseName: aredn-tls-test
  chart:
    spec:
      chart: app
      sourceRef:
        kind: HelmRepository
        name: usa-reddragon
        namespace: flux-system
      version: '0.1.30'
      interval: 60m0s
  valuesFrom:
    - kind: ConfigMap
      name: chart-values
